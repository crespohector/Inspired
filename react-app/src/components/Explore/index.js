import React from 'react';
import { NavLink } from 'react-router-dom';

import "./Explore.css";

const Explore = () => {
    //if no user or user is not login then redirect to splash page
    return (
        <>
            <h1>This is the Explore page</h1>
        </>
    );
}

export default Explore;
